package com.joaomantellato.calculadorajoaomantellato.ui.theme

import androidx.compose.ui.graphics.Color

// Fundo principal e Superfície do App (Off-White / Cinza Minimalista)
val MinimalBackground = Color(0xFFF7F8FA)
val MinimalSurface = Color(0xFFFFFFFF)

// Visor da Calculadora (Container suave cinza claro)
val MinimalDisplayBackground = Color(0xFFECEFF1)
val MinimalDisplayText = Color(0xFF1E293B)

// Botões Numéricos (Neutros e sutis)
val MinimalNumericButton = Color(0xFFFFFFFF)
val MinimalNumericText = Color(0xFF1E293B)

// Botões de Operação e Funções (Preto/Grafite elegante)
val MinimalOperatorButton = Color(0xFF1E293B)
val MinimalOperatorText = Color(0xFFFFFFFF)

// Botões em destaque secundário (Sub-funções)
val MinimalFunctionButton = Color(0xFFE2E8F0)
val MinimalFunctionText = Color(0xFF334155)

// Botões de Limpeza / Erro (Sutil destaque em tom terracota suave)
val MinimalClearButton = Color(0xFFEF4444)
val MinimalClearText = Color(0xFFFFFFFF)